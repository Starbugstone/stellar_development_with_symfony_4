{% extends 'base.html.twig' %}
{% block title %}
    Read: {{ title }}
{% endblock %}
{% block body %}
    <pre>
    {{  dump(app) }}
    </pre>
    <h1>{{ title }}</h1>

    <h2>Comments</h2>
    {% for comment in comments %}
        <li>{{ comment }}</li>
    {% endfor %}

{% endblock %}